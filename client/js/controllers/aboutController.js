app.controller("aboutController", [
    "$scope",
    "$http",
    "$location",
    "$window",
    /* eslint-disable no-unused-vars */
    function ($scope,
              $http,
              $location,
              $window) {
    /* eslint-enable no-unused-vars */

    }
]);
